[package]
name = "option_create"
version = "0.1.0"
description = "Option creation and validation program"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
no-entrypoint = []
no-idl = []
cpi = ["no-entrypoint"]

[dependencies]
anchor-lang = "0.30.1"
common = { path = "../common" }
